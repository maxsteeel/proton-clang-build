name: Build & upload toolchain
on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: buildjet-32vcpu-ubuntu-2204-arm
    steps:
      - name: Download scripts
        uses: actions/checkout@v6

      - name: Deploy builder
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          GIT_AUTHOR_NAME: ${{ secrets.GIT_NAME }}
          GIT_AUTHOR_EMAIL: ${{ secrets.GIT_EMAIL }}
          GIT_NAME: ${{ secrets.GIT_NAME }}
          GIT_EMAIL: ${{ secrets.GIT_EMAIL }}
          GIT_USER: ${{ secrets.GIT_NAME }}
          GH_NAME: ${{ secrets.GIT_NAME }}
          GH_BUILD_REPO: proton-clang-build
          GH_REL_REPO: proton-12-arm
        run: |
          ci/build_agent.sh
